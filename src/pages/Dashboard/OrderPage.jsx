import React from "react";
import SectionTitle from "../../components/SectionTitle";

const OrderPage = () => {
  return (
    <div>
      <SectionTitle heading={"Order Page"} />
    </div>
  );
};

export default OrderPage;
